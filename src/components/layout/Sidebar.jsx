function Mark() { return <div className="brand-mark" aria-hidden="true"><span /><span /><span /></div> }
export function Sidebar({ active, setActive, open, setOpen }) {
  const navGroups = [{ label: null, items: [['⌂', 'Dashboard']] }, { label: 'Content', items: [['▣', 'Projects'], ['▤', 'Blog Posts'], ['➤', 'Social Posts'], ['⌕', 'Research / Trending']] }, { label: 'Manage', items: [['▱', 'Technologies'], ['☆', 'Skills']] }, { label: 'System', items: [['⚙', 'Settings']] }]
  return <>{open && <button className="scrim" aria-label="Close navigation" onClick={() => setOpen(false)} />}<aside className={`sidebar ${open ? 'is-open' : ''}`}><div className="brand"><Mark /><div><strong>NAKIRI</strong><small>ADMIN</small></div></div><nav aria-label="Main navigation">{navGroups.map((group, index) => <div className="nav-group" key={group.label || index}>{group.label && <p className="nav-label">{group.label}</p>}{group.items.map(([icon, name]) => <button key={name} className={`nav-item ${active === name ? 'active' : ''}`} onClick={() => { setActive(name); setOpen(false) }}><span className="nav-icon">{icon}</span><span>{name}</span></button>)}</div>)}</nav><div className="sidebar-bottom"><button className="profile"><div className="avatar">C</div><div className="profile-copy"><strong>Christian</strong><small>Administrator</small></div><span className="chevron">⌄</span></button><button className="portfolio"><span>←</span> Back to Portfolio</button></div></aside></>
}
export default Sidebar
